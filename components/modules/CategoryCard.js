import React from 'react';
import Card from './Card';

const CategoryCard = (props) => {
    const {
        name,
        model,
        year,
        distance,
        location,
        image,
        price,
        description,
      } = props;
    return (
        <div>
        </div>
    );
};

export default CategoryCard;